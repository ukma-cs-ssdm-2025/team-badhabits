# Атрибути якості API

## Огляд
Цей документ визначає атрибути якості для **Wellity** — кросплатформенного мобільного застосунку для фітнесу та відстеження звичок. Документ встановлює вимірювані стандарти для забезпечення відповідності нашого Custom Backend API та системи в цілому вимогам продуктивності, безпеки, надійності та зручності використання, визначеним у [requirements.md](../requirements/requirements.md).

**Контекст архітектури:** Wellity використовує Client-Server архітектуру з Flutter клієнтом, Firebase (BaaS) для базових сервісів та Custom Backend для складної бізнес-логіки. Атрибути якості охоплюють всі компоненти системи.

## Атрибути якості

### 1. Продуктивність
**Визначення:** Система Wellity повинна забезпечувати швидку відповідь на дії користувача (фіксація звичок, workout sessions) та ефективно обробляти паралельні запити для підтримки плавного користувацького досвіду при відстеженні тренувань та звичок у реальному часі.

**Зв'язок з вимогами:**
- **PERF-001:** Час відповіді для фіксації habit trackers або завершення workout session ≤ 1 секунди
- **PERF-002:** Час запуску застосунку ≤ 3 секунди
- **PERF-003:** Візуалізація статистики ≤ 2 секунди для 365 днів даних
- **PERF-004:** Генерація адаптивного тренування ≤ 3 секунди після оцінки користувача

**Метрики:**
| Метрика | Цільове значення | Метод вимірювання | Пріоритет |
|---------|------------------|-------------------|-----------|
| Час відповіді API (p95) | < 500мс | Custom logging middleware у Custom Backend | Високий |
| Час відповіді API (p99) | < 1000мс | APM (Firebase Performance Monitoring) | Високий |
| Firebase Firestore запити | < 300мс середній | Firebase Performance SDK | Високий |
| Адаптивна генерація тренування | < 3с | Логування у Custom Backend | Високий |
| Завантаження статистики (365 днів) | < 2с | Flutter Performance overlay | Високий |
| Час запуску Flutter додатку | < 3с | Flutter DevTools Timeline | Середній |
| Синхронізація workout session | < 2с між пристроями | Firebase Realtime Database timestamps | Критичний |

**Інструменти вимірювання:**
- **Firebase Performance Monitoring** (безкоштовний) для моніторингу клієнта та Firebase операцій
- **Custom Express.js middleware** для вимірювання часу відповіді Custom Backend API
- **Flutter DevTools** для профілювання UI performance
- **Firestore Performance Monitoring** для аналізу запитів до БД
- **Postman/k6** (безкоштовні версії) для базового навантажувального тестування

**Підхід до тестування:**
- Запуск Flutter Performance профілювання щотижня під час розробки
- Моніторинг Firebase Performance Monitoring у реальному часі після деплою
- Навантажувальне тестування Custom Backend з 50-100 одночасних користувачів перед релізом
- Налаштування Firebase Performance alerts для критичних операцій

**Критерії прийняття:**
- 95% API запитів Custom Backend виконуються < 500мс
- Firestore операції (CRUD habit trackers) виконуються < 300мс
- Workout session синхронізація між пристроями < 2с (REL-004)
- Адаптивні тренування генеруються < 3с (PERF-004)
- Візуалізація статистики завантажується < 2с для річних даних (PERF-003)
- Застосунок запускається < 3с на середніх пристроях (PERF-002)

---

### 2. Безпека
**Визначення:** Wellity як застосунок для відстеження особистих даних (тренування, звички, платежі) повинен захищати конфіденційну інформацію користувачів, запобігати несанкціонованому доступу та відповідати стандартам безпеки для фінансових транзакцій (платні тренування).

**Зв'язок з вимогами:**
- **SEC-001:** Шифрування паролів (SHA-256 мінімум)
- **SEC-002:** Передача даних тільки через HTTPS
- **SEC-003:** Тайм-аут сеансу 24 години неактивності
- **SEC-004:** PCI DSS compliance для платіжного gateway (платні тренування)

**Метрики:**
| Метрика | Цільове значення | Метод вимірювання | Пріоритет |
|---------|------------------|-------------------|-----------|
| Вразливості залежностей | 0 критичних, 0 високих | npm audit (backend), Flutter analyze | Критичний |
| Успішність Firebase Auth | > 99.5% | Firebase Auth logs | Високий |
| Підозріла активність входу | < 5% користувачів з 5+ невдалими спробами/годину | Firebase Auth rate limiting logs | Високий |
| HTTPS покриття | 100% всіх з'єднань | Firebase Hosting + Custom Backend SSL | Критичний |
| Шифрування даних у спокої | 100% | Firebase Firestore автоматичне шифрування | Критичний |
| PCI DSS compliance | 100% платіжних операцій | Stripe/PayPal integration аудит | Критичний |
| Токени автентифікації | Ротація кожні 24 години | Firebase ID tokens (автоматично) | Високий |

**Пояснення метрики "Підозріла активність входу":**

Ця метрика призначена для виявлення brute-force атак та зловмисної активності, а не звичайних помилок користувачів (забув пароль, друкарська помилка).

**Пороги автоматичного реагування:**
- **5+ невдалих спроб за годину** → Firebase Auth показує CAPTCHA
- **10+ невдалих спроб за годину** → Тимчасове блокування акаунту на 15 хвилин
- **20+ невдалих спроб за годину** → Блокування на 1 годину + email сповіщення користувачу про підозрілу активність

**Мета:** Захистити акаунти від автоматизованих атак (credential stuffing, brute-force), зберігаючи зручність для легітимних користувачів.

**Інструменти вимірювання:**
- **npm audit** та **Flutter analyze** для сканування вразливостей залежностей
- **Firebase Security Rules** для контролю доступу до Firestore/Realtime Database
- **Snyk** (безкоштовна версія) для continuous security scanning
- **Firebase Auth Logs** для моніторингу спроб входу та виявлення аномалій
- **Stripe/PayPal Security Dashboard** для compliance перевірок
- **SSL Labs** (безкоштовний) для перевірки HTTPS конфігурації

**Підхід до тестування:**
- Щотижневі автоматизовані npm audit перевірки у CI/CD
- Щомісячне ручне тестування Firebase Security Rules
- Перевірка PCI DSS compliance через Stripe/PayPal перед запуском платежів
- Code review для всіх змін у автентифікації та платежах
- Використання Firebase App Check для захисту від зловмисних запитів
- Моніторинг Firebase Auth logs для виявлення патернів атак

**Критерії прийняття:**
- Нуль критичних або високої важливості вразливостей у production (npm audit, Flutter analyze)
- Firebase Authentication вимагає верифікації email для всіх користувачів
- Усі дані передаються через HTTPS (Firebase Hosting + Custom Backend SSL)
- Firestore Security Rules блокують несанкціонований доступ до чужих habit trackers та workout sessions
- Платіжний gateway (Stripe/PayPal) відповідає PCI DSS (SEC-004)
- Firebase ID tokens автоматично оновлюються кожні 60 хвилин, сесія закінчується через 24 години неактивності (SEC-003)
- Rate limiting на Firebase Auth налаштовано:
  - 5 спроб → CAPTCHA
  - 10 спроб → блокування 15 хв
  - 20 спроб → блокування 1 год + email alert
- < 5% користувачів мають 5+ невдалих спроб входу за годину (індикатор низького рівня атак)

---

### 3. Надійність
**Визначення:** Wellity повинен бути стабільно доступним для користувачів, які відстежують щоденні звички та тренування, забезпечувати offline-first роботу та гарантувати синхронізацію даних між пристроями без втрат.

**Зв'язок з вимогами:**
- **REL-001:** Uptime ≥ 99.0% під час пікових годин (6:00-22:00)
- **REL-002:** Успішність синхронізації даних ≥ 98% на всіх платформах
- **REL-003:** Обробка переривання мережі без втрати даних
- **REL-004:** Workout session синхронізація ≤ 2 секунди

**Метрики:**
| Метрика | Цільове значення | Метод вимірювання | Пріоритет |
|---------|------------------|-------------------|-----------|
| Uptime (пікові години 6:00-22:00) | ≥ 99.0% | Firebase Status Dashboard + UptimeRobot (безкоштовний) | Критичний |
| Успішність Firestore синхронізації | ≥ 98% | Firebase SDK offline logs | Критичний |
| Workout session realtime sync | ≤ 2с затримка | Firebase Realtime Database timestamps | Критичний |
| Помилки Custom Backend API | < 1% запитів | Express.js error logging + Sentry (безкоштовний tier) | Високий |
| Offline operations успішність | 100% для habit trackers | Firebase offline persistence перевірка | Високий |
| Час відновлення після збою (MTTR) | < 30 хвилин | Manual incident logs | Середній |

**Інструменти вимірювання:**
- **Firebase Status Dashboard** для моніторингу Firebase інфраструктури
- **UptimeRobot** (безкоштовний план до 50 моніторів) для Custom Backend uptime
- **Firebase SDK Offline Logging** для відстеження синхронізації
- **Sentry** (безкоштовний tier) для error tracking у Custom Backend
- **Firebase Crashlytics** для відстеження збоїв Flutter додатку
- **Manual logging** для incident response та MTTR

**Підхід до тестування:**
- Моніторинг Firebase Status Dashboard 24/7 (автоматичні email сповіщення)
- UptimeRobot перевірка Custom Backend кожні 5 хвилин
- Щотижневе тестування offline scenarios у Flutter (airplane mode)
- Щомісячне тестування синхронізації між кількома пристроями
- Симуляція мережевих переривань під час workout sessions

**Критерії прийняття:**
- Firebase та Custom Backend доступні ≥ 99.0% часу в пікові години 6:00-22:00 (REL-001)
- Firestore offline persistence працює коректно — habit trackers створюються offline та синхронізуються після відновлення мережі (REL-002)
- Firebase Realtime Database синхронізує workout sessions між пристроями ≤ 2с (REL-004)
- Workout sessions автоматично зупиняються з попередженням при втраті з'єднання (REL-003)
- Custom Backend API error rate < 1%
- Firebase Crashlytics показує < 1% crash rate для Flutter додатку
- Середній час відновлення після інцидентів < 30 хвилин

---

### 4. Зручність використання
**Визначення:** Custom Backend API Wellity повинен бути добре задокументованим та простим для інтеграції Flutter розробників команди, забезпечувати консистентні відповіді та зрозумілі повідомлення про помилки для швидкої розробки нових функцій.

**Зв'язок з вимогами:**
- **USAB-001:** Перший habit tracker або тренування за 5 хвилин
- **USAB-003:** Критичні дії максимум за 3 дотики/кліки
- ADR-003: REST API для Custom Backend

**Метрики:**
| Метрика | Цільове значення | Метод вимірювання | Пріоритет |
|---------|------------------|-------------------|-----------|
| API Documentation покриття | 100% ендпоінтів Custom Backend | Swagger/OpenAPI generator | Високий |
| Консистентність API responses | 100% (єдиний формат) | Manual code review + linting | Високий |
| Час онбордингу нового розробника | < 30 хвилин до першого API виклику | Team survey | Середній |
| Зрозумілість error messages | > 4.0/5.0 | Developer feedback | Середній |
| Breaking changes per release | 0 (API versioning) | Release notes review | Високий |
| Flutter integration приклади | ≥ 3 ендпоінти | Code examples у документації | Високий |

**Інструменти вимірювання:**
- **Swagger UI / OpenAPI 3.0** для автоматичної документації Custom Backend API
- **Spectral** (безкоштовний) для linting OpenAPI spec консистентності
- **Postman Collections** для тестування та документування прикладів API
- **GitHub Issues** для збору feedback команди розробників
- **Team retrospectives** для оцінки DX (Developer Experience)

**Підхід до тестування:**
- Генерація OpenAPI spec автоматично з Express.js routes
- Щотижневий огляд API змін на team meetings
- Code review для перевірки консистентності response formats
- Створення Postman Collection для всіх Custom Backend ендпоінтів
- Документування Flutter integration прикладів у README

**Критерії прийняття:**
- Всі Custom Backend ендпоінти задокументовані у OpenAPI 3.0 spec з прикладами
- Swagger UI доступний на `/api-docs` для інтерактивної документації
- Єдиний формат відповідей: `{ "data": {...}, "error": null }` або `{ "data": null, "error": {...} }`
- Error messages містять чіткий `errorCode`, `message` та `details`
- Flutter приклади коду доступні для адаптивних тренувань, платежів та статистики
- API versioning (`/api/v1/...`) для уникнення breaking changes
- Postman Collection з прикладами для всіх ендпоінтів
- Developer onboarding guide у README з швидким стартом

---

## Стратегія моніторингу

### Безперервний моніторинг
- **Firebase Performance Monitoring:** Автоматичний збір метрик клієнта та Firebase операцій у реальному часі
- **UptimeRobot:** Перевірка доступності Custom Backend кожні 5 хвилин
- **Sentry Dashboard:** Моніторинг помилок Custom Backend у реальному часі
- **Firebase Crashlytics:** Відстеження збоїв Flutter додатку
- **Щотижневі team sync meetings:** Огляд ключових метрик та трендів команди BadHabits

### Пороги сповіщень
| Метрика | Попередження | Критичний |
|---------|--------------|-----------|
| Custom Backend API response time (p95) | > 700мс | > 1000мс |
| Custom Backend error rate | > 0.5% | > 1% |
| Firebase operations time | > 400мс | > 500мс |
| Uptime (Custom Backend) | < 99.5% | < 99.0% |
| npm audit vulnerabilities | Будь-яка висока | Будь-яка критична |
| Flutter crash rate | > 0.5% | > 1% |

**Інструменти сповіщень:**
- Firebase Performance alerts (email)
- UptimeRobot email notifications
- Sentry Slack/Email integration
- GitHub Actions failures (CI/CD)

### Цикл перегляду
- **Щоденно (automated):** GitHub Actions CI перевірки (npm audit, Flutter analyze, unit tests)
- **Щотижня (team meeting):** Огляд Firebase Performance metrics, Sentry errors, UptimeRobot uptime
- **Після кожного спринту:** Ретроспектива з аналізом досягнення цільових метрик якості
- **Щомісяця:** Оновлення OpenAPI документації, review Firebase Security Rules
- **Щокварталу:** Комплексний аудит безпеки (npm audit, Snyk scan, Firebase Rules review)

---

## Контрольні точки якості

### Контрольні точки перед Production
Перед розгортанням у production всі наступні перевірки мають бути виконані:

**Продуктивність:**
- [ ] Flutter Performance профілювання виконано — app startup < 3с, scroll performance 60 FPS
- [ ] Firebase Performance baseline встановлено для критичних операцій (habit tracking, workout sessions)
- [ ] Custom Backend навантажувальне тестування з k6: 50-100 одночасних користувачів, p95 < 500мс
- [ ] Firestore індекси створені для всіх складних queries (статистика, фільтрація тренувань)
- [ ] Адаптивна генерація тренування протестована — < 3с для типових сценаріїв (PERF-004)

**Безпека:**
- [ ] npm audit показує 0 критичних/високих вразливостей у Custom Backend
- [ ] Flutter analyze не показує security warnings
- [ ] Firebase Security Rules перевірені — доступ до habit trackers тільки для власника
- [ ] Firebase Security Rules для workout sessions — блокування паралельних сесій реалізовано (FR-013)
- [ ] HTTPS налаштовано для Custom Backend (SSL сертифікат, SSL Labs A+ grade)
- [ ] Stripe/PayPal integration протестовано у sandbox mode (SEC-004, PCI DSS)
- [ ] Firebase App Check увімкнено для захисту від bot attacks
- [ ] Firebase ID tokens валідація у Custom Backend middleware

**Надійність:**
- [ ] Firebase Firestore offline persistence протестовано — habit trackers працюють offline
- [ ] Firebase Realtime Database workout session синхронізація < 2с протестовано між 2 пристроями
- [ ] UptimeRobot налаштовано для Custom Backend health endpoint (`/health`)
- [ ] Sentry error tracking інтегровано у Custom Backend
- [ ] Firebase Crashlytics інтегровано у Flutter додаток
- [ ] Error handling протестовано для мережевих переривань — workout sessions зупиняються з попередженням (REL-003)
- [ ] Firestore backups налаштовані (Firebase автоматичні backups увімкнені)

**Зручність використання:**
- [ ] OpenAPI 3.0 spec згенеровано для всіх Custom Backend ендпоінтів
- [ ] Swagger UI доступний на `/api-docs` з прикладами запитів
- [ ] Postman Collection створено для всіх API ендпоінтів
- [ ] Flutter integration приклади додані у `/docs/api/` для адаптивних тренувань, платежів, статистики
- [ ] README оновлено з інструкціями для розгортання Custom Backend
- [ ] API versioning `/api/v1/...` впроваджено для уникнення breaking changes
- [ ] Error messages перевірені — містять `errorCode`, `message`, `details`

### Контрольні точки в Production
Безперервний моніторинг та автоматичні дії:

- **Performance Degradation:** Firebase Performance alert якщо Firestore operations > 500мс протягом 10 хвилин
- **High Error Rate:** Sentry alert якщо Custom Backend error rate > 1% протягом 5 хвилин
- **Downtime Response:** UptimeRobot email якщо Custom Backend недоступний > 5 хвилин
- **Security Incident:** Негайне розслідування при підозрілій активності у Firebase Auth logs (multiple failed logins)
- **Crash Rate Alert:** Firebase Crashlytics alert якщо Flutter crash rate > 1%

---

## Відстеження покращень

### Поточний базовий рівень (станом на 2025-01-20)
| Атрибут | Поточний | Цільовий | Статус |
|---------|----------|----------|--------|
| Custom Backend API p95 | TBD | < 500мс | 🔄 Очікує Custom Backend MVP |
| Firebase Firestore operations | TBD | < 300мс | 🔄 Вимірюється після інтеграції |
| Flutter app startup | TBD | < 3с | 🔄 Вимірюється у розробці |
| npm audit vulnerabilities | 0 | 0 критичних | ✅ Перевірено |
| Firestore offline sync успішність | TBD | ≥ 98% | 🔄 Тестується |
| OpenAPI documentation coverage | 0% | 100% | 🔄 Custom Backend у розробці |
| Firebase Crashlytics crash rate | TBD | < 1% | 🔄 Після production deploy |

### План покращень
1. **Спринт 1-2 (Поточний):** Встановити базові вимірювання для Flutter + Firebase операцій
   - Інтегрувати Firebase Performance Monitoring
   - Виміряти app startup time та Firestore operations latency
   - Налаштувати Flutter Crashlytics

2. **Спринт 3-4 (Custom Backend MVP):** Встановити baseline для Custom Backend API
   - Розгорнути Custom Backend з Express.js
   - Інтегрувати Sentry error tracking
   - Налаштувати UptimeRobot моніторинг
   - Згенерувати перший OpenAPI spec

3. **Спринт 5-6 (Security & Testing):** Підвищити security та reliability standards
   - Налаштувати Firebase Security Rules для habit trackers та workout sessions
   - Інтегрувати Stripe sandbox для платежів
   - Провести навантажувальне тестування (k6, 50-100 користувачів)
   - Протестувати offline scenarios

4. **Спринт 7+ (Continuous Optimization):** Безперервне покращення та моніторинг
   - Оптимізувати Firestore queries на основі Firebase Performance insights
   - Додати Firestore composite indices для складних queries
   - Покращити Custom Backend response times
   - Розширити OpenAPI documentation з більше прикладів

---

## Посилання

### Внутрішні документи проекту
- [Функціональні та нефункціональні вимоги](../requirements/requirements.md) — джерело NFR (PERF, REL, SEC, USAB)
- [Користувацькі історії](../requirements/user-stories.md) — контекст використання функцій
- [High-Level Architecture](../architecture/high-level-design.md) — Client-Server архітектура з Firebase та Custom Backend
- [ADR-002: Вибір бази даних](../decisions/ADR-002-database-choice.md) — Firebase Firestore + Realtime Database
- [ADR-003: Дизайн API](../decisions/ADR-003-api-design.md) — REST API для Custom Backend
- [Team Charter](../../TeamCharter.md) — процеси команди BadHabits, review cycles, комунікація

### Зовнішні стандарти та довідкові матеріали
- [ISO 25010 Quality Model](https://iso25000.com/index.php/en/iso-25000-standards/iso-25010) — міжнародний стандарт якості ПЗ
- [OpenAPI 3.0 Specification](https://swagger.io/specification/) — стандарт документування REST API
- [Firebase Performance Best Practices](https://firebase.google.com/docs/perf-mon/best-practices) — гайди Firebase Performance
- [Firebase Security Rules Guide](https://firebase.google.com/docs/rules) — захист Firestore та Realtime Database
- [OWASP Mobile Security](https://owasp.org/www-project-mobile-security/) — стандарти безпеки мобільних додатків
- [PCI DSS Quick Reference Guide](https://www.pcisecuritystandards.org/) — стандарти безпеки платежів
- [Flutter Performance Best Practices](https://docs.flutter.dev/perf/best-practices) — оптимізація Flutter додатків
